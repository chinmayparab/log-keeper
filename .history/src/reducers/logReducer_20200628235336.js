const initialState = {
  logs: null,
  current: null,
  loading: false,
  error: null,
};

export default (state, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
};
