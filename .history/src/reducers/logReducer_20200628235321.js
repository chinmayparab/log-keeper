const initialState = {
  logs: null,
  current: null,
  loading: false,
};

export default (state, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
};
